name: Eiffel CI

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

env:
  ISE_EIFFEL: /usr/local/eiffel
  ISE_PLATFORM: linux-x86-64
  ISE_LIBRARY: /usr/local/eiffel
  SIMPLE_EIFFEL: /opt/simple_eiffel

jobs:
  build-and-test:
    runs-on: [self-hosted, eiffel]
    timeout-minutes: 30

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Verify EiffelStudio
      run: |
        export PATH="$ISE_EIFFEL/studio/spec/$ISE_PLATFORM/bin:$PATH"
        echo "ISE_EIFFEL: $ISE_EIFFEL"
        ec -version

    - name: Setup dependencies
      run: |
        mkdir -p $SIMPLE_EIFFEL
        cd $SIMPLE_EIFFEL
        # All required dependencies for simple_k8s
        DEPS="simple_base64 simple_decimal simple_json simple_yaml simple_toml simple_xml simple_compression simple_regex simple_csv simple_file simple_http simple_logger simple_testing simple_codec simple_env simple_process simple_datetime simple_archive simple_config simple_app_api simple_ci simple_foundation_api simple_uuid simple_mmap simple_system simple_watcher"
        for lib in $DEPS; do
          if [ ! -d "$lib" ]; then
            echo "Cloning $lib..."
            git clone --depth 1 https://github.com/simple-eiffel/$lib.git || true
          fi
        done

    - name: Pre-compile C libraries
      run: |
        cd $SIMPLE_EIFFEL
        for lib in simple_env simple_process simple_mmap simple_system simple_watcher; do
          if [ -d "$lib/Clib" ]; then
            echo "Compiling $lib..."
            cd "$lib/Clib"
            for cfile in *.c; do
              [ -f "$cfile" ] && gcc -c -fPIC "$cfile" -o "${cfile%.c}.o" 2>/dev/null || true
            done
            cd $SIMPLE_EIFFEL
          fi
        done

    - name: Compile simple_k8s
      run: |
        export PATH="$ISE_EIFFEL/studio/spec/$ISE_PLATFORM/bin:$PATH"
        cd $GITHUB_WORKSPACE
        rm -rf EIFGENs
        ec -batch -config simple_k8s.ecf -target simple_k8s_tests -c_compile
      timeout-minutes: 20

    - name: Run tests
      run: |
        cd $GITHUB_WORKSPACE
        EXECUTABLE=$(find EIFGENs -type f -executable 2>/dev/null | grep -v "\.o$" | head -1)
        if [ -n "$EXECUTABLE" ]; then
          echo "Running: $EXECUTABLE"
          "$EXECUTABLE"
        else
          echo "ERROR: No executable found"
          find EIFGENs -type f | head -20
          exit 1
        fi
      timeout-minutes: 10
